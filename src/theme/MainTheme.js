import { createTheme } from '@mui/material/styles'

const mainTheme = createTheme({
  palette: {
    primary: {
      main: '#e31e24'
    },
    secondary: {
      main: '#000000'      
    },
  },
  
});

export default mainTheme